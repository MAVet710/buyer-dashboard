# Public production frontend is served at ops.doobielogic.io by the PC-hosted Caddy edge.
# Keep API calls same-origin so remote browsers never try to call their own localhost.
VITE_API_URL=
# Browser authentication stays on the existing DoobieLogic Supabase project.
# This must match the SUPABASE_URL used by the PC-hosted FastAPI backend.
VITE_SUPABASE_URL=https://fovxtygwcxubjzjgovva.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=existing-enabled-publishable-or-anon-key
